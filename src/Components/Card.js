import styled from "styled-components";

const Card = styled.div`
  width: 350px;
  height: 350px;
  margin: 0 auto;
  border-radius: 30px;
  background-color: var(--Dark-Blue);
  padding: 30px;
  color: var(--White);
`;

export default Card;
